#!/usr/bin/env ruby
# frozen_string_literal: true

repo_root = File.expand_path("..", __dir__)
test_files = Dir.glob("{scripts,e2e}/test/**/*_test.rb", base: repo_root).sort

test_files.each { |path| require File.join(repo_root, path) }
