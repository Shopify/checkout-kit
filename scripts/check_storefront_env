#!/usr/bin/env bash

set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

if ! "${ROOT_DIR}/scripts/setup_storefront_env" --check; then
  echo "Run 'dev up' to provision sample storefront configuration." >&2
  exit 1
fi
