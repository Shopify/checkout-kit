appId: ${E2E_APP_ID}
---
- scrollUntilVisible:
    element:
      text: "^(Pay now|Complete order)$"
    direction: DOWN
    visibilityPercentage: 90
- tapOn:
    text: "^(Pay now|Complete order)$"
    enabled: true
