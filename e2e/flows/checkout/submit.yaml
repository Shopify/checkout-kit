appId: ${APP_ID}
---
- scrollUntilVisible:
    element:
      text: "^(Pay now|Complete order)$"
    direction: DOWN
    visibilityPercentage: 90
    timeout: 10000
- tapOn:
    text: "^(Pay now|Complete order)$"
    enabled: true
