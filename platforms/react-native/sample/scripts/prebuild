#!/usr/bin/env bash

set -e

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
SAMPLE_DIR="$(cd "$SCRIPT_DIR/.." && pwd)"
platform="${1:-all}"

cd "$SAMPLE_DIR"
pnpm exec expo prebuild --platform "$platform" --no-install --clean
