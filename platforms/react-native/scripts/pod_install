#!/usr/bin/env bash

set -e

for arg in "$@"; do
  if [ "$arg" = "--local" ]; then
    export USE_LOCAL_SDK=1
  fi
done

cd sample/ios
bundle check || bundle install
bundle exec pod install --repo-update
