#!/bin/bash

OUTPUT_FILE=${1:-modules/@shopify/checkout-sheet-kit/package.snapshot.json}

(cd modules/@shopify/checkout-sheet-kit && pnpm pack --dry-run --json) | jq ".files | map(.path)" > $OUTPUT_FILE
