#!/bin/bash

OUTPUT_FILE=${1:-modules/@shopify/checkout-kit-react-native/package.snapshot.json}

(cd modules/@shopify/checkout-kit-react-native && pnpm pack --dry-run --json) | jq ".files | map(.path)" > $OUTPUT_FILE
