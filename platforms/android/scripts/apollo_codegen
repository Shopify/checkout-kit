#!/bin/bash
set -euo pipefail

cd samples/CheckoutKitAndroidDemo

echo "🔄 Running codegen for CheckoutKitAndroidDemo..."
./gradlew :app:generateApolloSources

if [ $? -eq 0 ]; then
  echo "✅ Codegen complete"
else
  echo "❌ Codegen failed"
  exit 1
fi

cd ../..
/opt/dev/bin/dev android lint
